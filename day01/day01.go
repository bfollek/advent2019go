package day01

// Part1 sums the frequency changes.
func Part1(fileName string) (int, error) {
	// freqs, err := loadFreqs(fileName)
	// if err != nil {
	// 	return 0, err
	// }
	// sum := 0
	// for _, n := range freqs {
	// 	sum += n
	// }
	// return sum, nil
	return 0, nil
}
