package day01_test

import "testing"

func TestPart1(t *testing.T) {
	// l := todo.List{}
	// taskName := "New Task"
	// l.Add(taskName)
	// if l[0].Task != taskName {
	// 	t.Errorf("Expected %q, got %q instead.", taskName, l[0].Task)
	// }
}
